# ResNet18-Banana-Ripeness-Classification
ResNet18 banana ripeness classification uses a ResNet18 convolutional neural network, fine‑tuned on banana images, to automatically predict discrete ripeness stages (for example, unripe, ripe, overripe) from a single photo.
